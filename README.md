SME Support Chatbot (Deterministic) — Buyer Info (2025)

IMPORTANT
This GitHub repository is INFORMATION-ONLY.
It intentionally contains NO source code.

The deliverable is provided separately as a sealed ZIP package (Windows-ready) that includes:
- Python + Flask demo
- Deterministic FAQ engine (rule-based)
- Content packs (language/sector)
- Portable runner scripts (run / verify)
- Buyer-grade proof (SHA256 + audit artifacts)

Executive Summary
Transferable non-SaaS digital asset: a deterministic customer support chatbot built with Python + Flask.
Designed to reduce repetitive support tickets with controlled, predictable answers (no hallucinations) and near-zero operating cost (no paid AI / no external APIs).

Use cases
- Resale as an asset
- Integration into a larger platform
- Optional SaaS conversion by the buyer (not included)

What exists in the deliverable ZIP
- Deterministic matching engine + safe fallback
- Flask local web demo
- Editable FAQ content (JSON)
- Language/sector content packs
- Logging (local)
- Verification helpers (structure checks + simple HTTP check)
- No secrets shipped (.env.example only)

Buyer verification (how you validate the ZIP)
A sealed ZIP is provided with:
- ZIP SHA256 (sidecar text file)
- Content manifest (SHA256SUMS of delivered files)
- Audit report (forbidden files/dirs must be NONE)

Request the sealed ZIP + SHA256 from the seller to run the demo locally.

License
See LICENSE.md.
